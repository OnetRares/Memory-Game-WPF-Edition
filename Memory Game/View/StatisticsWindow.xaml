<Window x:Class="Memory_Game.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Memory_Game.ViewModel"
        Title="Statisticile Jucătorilor" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#2C3E50"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding CloseWindowCommand}" CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"/>
    </Window.InputBindings>

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style x:Key="ItemTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
          
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" BorderBrush="#BDC3C7" BorderThickness="2" Padding="15" Background="#2C3E50">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <TextBlock Text="Player Statistics" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center"/>

            
            <ListBox ItemsSource="{Binding PlayerStatistics}" Grid.Row="1" Margin="10" Background="#2C3E50" BorderThickness="1" BorderBrush="#BDC3C7">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border CornerRadius="5" Background="#ECF0F1" Padding="12" Margin="6" BorderBrush="#BDC3C7" BorderThickness="1">
                            <TextBlock Text="{Binding}" Style="{StaticResource ItemTextStyle}" TextWrapping="Wrap"/>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
